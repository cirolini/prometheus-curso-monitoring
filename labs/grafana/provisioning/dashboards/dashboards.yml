apiVersion: 1

providers:
  - name: lab
    orgId: 1
    folder: 'Curso Prometheus'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
